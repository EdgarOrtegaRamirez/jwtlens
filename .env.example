# Environment variables for JWTLens
# Copy this file to .env and fill in your values
# NEVER commit the actual .env file

# (No required env vars — JWTLens is fully self-contained CLI)
# Token is passed as CLI argument, --file, or stdin.
# Secret key for validation is passed via --secret flag.